# Copy to `.env` and fill in. Never commit real secrets.
VITE_API_URL=http://localhost:8080

# TMDB (optional): posters / hero — https://www.themoviedb.org/settings/api
# Use either Bearer read token OR API key (code prefers Bearer when set).
VITE_TMDB_READ_ACCESS_TOKEN=
VITE_TMDB_API_KEY=
